<%- include('./partials/header.ejs') %>
<div class="container mx-auto bg-green-50 p-6">
    <div class="max-w-4xl mx-auto  p-8  rounded-2xl">
      <h2 class="text-4xl font-bold text-black mb-6 ">Create New Hisaab</h2>
      <form action="/hisaab/create" method="post" class="space-y-5">
        <!-- Title -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Title</label>
          <input
            name="title"
            type="text"
            placeholder="Shopping Hisaab, Ghar ka Kharch..."
            class="w-full px-4 py-4  rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          />
        </div>
  
        <!-- Description -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
          <textarea
            name="description"
            placeholder="Write a Hissab here..."
            class="w-full resize-none px-4 py-2 rounded-lg h-32 focus:outline-none focus:ring-2 focus:ring-blue-400"
          ></textarea>
        </div>
  
        <!-- Encryption -->
        <div class="flex items-start space-x-3">
          <input name="encrypted" type="checkbox" class="mt-1 form-checkbox text-blue-500" />
          <div class="flex-1">
            <label class="text-sm text-gray-700">Encrypt this file</label>
            <input
              name="passcode"
              type="password"
              placeholder="Enter passcode"
              class="mt-2 w-full px-4 py-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
            />
          </div>
        </div>
  
        <!-- Shareable -->
        <div class="flex items-center space-x-3">
          <input name="shareable" type="checkbox" class="form-checkbox text-blue-500" />
          <label class="text-sm text-gray-700">Make this file shareable</label>
        </div>
  
        <!-- Edit Permission -->
        <div class="flex items-center space-x-3">
          <input name="editpermissions" type="checkbox" class="form-checkbox text-blue-500" />
          <label class="text-sm text-gray-700">Allow others to edit</label>
        </div>
  
        <!-- Submit Button -->
        <div>
          <button
            type="submit"
            class="w-[200px] bg-blue-600 text-center py-3 mt-5 hover:bg-blue-700 text-white font-medium py-2 rounded-lg transition duration-200"
          >
             Create New Hisaab
          </button>
        </div>
      </form>
    </div>
  </div>
  
    <%- include('./partials/footer.ejs') %>